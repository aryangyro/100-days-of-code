Built a desktop application using Python and Tkinter that fetches and displays real-time Kanye West quotes from a public REST API. Implemented HTTP requests using the requests library, parsed JSON responses, and dynamically updated UI elements on user interaction. The application initializes with a quote at startup and allows users to fetch new quotes via button clicks, demonstrating API consumption, event-driven programming, and GUI state management.
